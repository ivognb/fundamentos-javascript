const [a, b, c, d] = [1, 3, 4, 6]

const soma = a + b + c + d
const subtracao = d - b
const multiplicacao = a * b
const divisao = d/a
const modulo = 0 % 2

console.log(soma, subtracao, multiplicacao ,divisao, modulo);